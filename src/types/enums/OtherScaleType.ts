/**
 * Scales that don't fit the 7-note diatonic model (no key signature, no tertian scale-degree
 * chords) - see {@link OtherScalePattern} and {@link OtherScaleInfo}.
 */
export enum OtherScaleType {
  WholeTone = "Whole Tone",
}
